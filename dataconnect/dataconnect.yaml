specVersion: "v1beta"
serviceId: "admin-test"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admin-test-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
